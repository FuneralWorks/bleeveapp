angular.module('Bleeveapp').controller('meetingRoomController', meetingRoomController);

homeController.$inject = ['$scope', 'GAuth', 'GApi', '$location', '$rootScope', 'GData'];

function meetingRoomController($scope, GAuth, GApi, $location, $rootScope, GData) {
    
}
